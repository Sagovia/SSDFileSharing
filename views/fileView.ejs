<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="Content-Security-Policy" content="default-src 'self'; script-src 'self' 'nonce-<%= nonce %>'; style-src 'self' 'nonce-<%= nonce %>';">
  <title>File Details - SSDFileSharing</title>
  <style nonce="<%= nonce %>">


  html, body {
      height: 100%;
    }

    body {
      flex-direction: column;
      min-height: 100%;
      font-family: Arial;
      margin: 0;
      display: flex;
      background-color: whitesmoke;
      color: dimgray;
    }

    .header {
      padding: 1.6rem 2rem;
      background: #005f99;
      text-align: center;
      color: whitesmoke;
    }
    .header h1 {
      font-size: 1.8rem;
      margin: 0;
    }

    .container {
      flex: 1;
      padding: 2rem;
      display: flex;
      flex-direction: column;
      align-items: center; 
    }
    .link-button {
      color: whitesmoke;
      padding: 0.75rem 1.5rem;
      background: #007bb5;
      border: none;
      cursor: pointer;
      border-radius: 4px;
      display: inline-block;
    }


    .card {
      background: white;
      max-width: 500px;
      margin-bottom: 2rem;
      border-radius: 10px;
      padding: 2rem;
      width: 100%;
    }
    .card h2 {
      margin-top: 0;
      color: #005f99;
    }

    a {
      color: #005f99;
      text-decoration: none;
    }

    ul {
      margin: 0;
      list-style: none;
      padding: 0;
    }
    ul li {
      margin-bottom: 0.6rem;
      font-size: 1.1rem;
    }
    footer {
      background: #005f99;
      text-align: right;
      font-size: 1rem;
      color: whitesmoke;
      padding: 1rem 2rem;
    }
  </style>
</head>
<body>
  <!-- Header -->
  <header class="header">
    <h1>File Details</h1>
  </header>

  <!-- Main Container -->
  <div class="container">
    <div class="card">
      <ul>

        <li><strong>Download link:</strong> <a href="<%= fileLink %>" target="_blank" rel="noopener noreferrer"><%= fileLink %></a></li>
        <li><strong>File name:</strong> <%= fileName %></li>
        <li><strong>Download count:</strong> <%= downloadCount %></li>
      </ul>
    </div>
    <div>
      <a class="link-button" href="/acc/home" rel="noopener noreferrer">Return home</a>
    </div>
  </div>

  <!-- Footer -->
  <footer>
    Uploading files since 2025
  </footer>
</body>
</html>
