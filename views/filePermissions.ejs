<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>File Permissions</title>
  <style>

  html, body {
      min-height: 100vh;
    } 

    body {
      flex-direction: column;
      margin: 0;
      font-family: Arial;
      display: flex;
      background-color: whitesmoke;
      color: dimgray;
    }
 
    header {
      background: #005f99;
      color: white;
      padding: 1.3rem 2rem;
      text-align: center;
    }
    header h1 {
      font-size: 2rem;
      margin: 0;
    }

    a {
      text-decoration: none;
      color: #005f99;
    }

  
    .container {
      flex-direction: column;
      padding: 2rem;
      align-items: center;
      flex: 1;
      display: flex;

    }

    .card {
      max-width: 500px;
      background: white;
      width: 100%;
      padding: 2rem;
      border-radius: 10px;
      margin-bottom: 1.7rem;
    }
    .card h2 {
      font-size: 1.5rem;
      margin: 0 0 1rem 0;
      color: #005f99;
    }


    form {
      flex-direction: column;
      display: flex;
    }

    form input[type="text"],
    form input[type="password"] {
      padding: 0.5rem;
      border-radius: 4px;
      margin-top: 0.5rem;
      font-size: 1rem;
    }
    form button {
      color: whitesmoke;
      margin-top: 1.6rem;
      padding: 0.7rem;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      font-size: 1rem;
      background-color: #005f99;
    }
    form label {
      color: #003d66;
      margin-top: 1rem;
      font-weight: bold;
    }
    form input[type="checkbox"] {
      margin-right: 0.5rem;
    }
    form button:hover {
      background-color: #004e80;
    }
    footer {
      background: #005f99;
      color: #fff;
      padding: 1rem 2rem;
      text-align: right;
      font-size: 0.9rem;
    }
    .info p {
      margin: 0.5rem 0;
      font-size: 1.1rem;
    }
  </style>
</head>
<body>
  <!-- Header -->
  <header>
    <h1>File Permissions</h1>
  </header>

  <!-- Main Container -->
  <div class="container">
    <div class="card">
      <!-- File Name and Current Permissions -->
      <h2><%= fileName %></h2>
      <div class="info">
        <p><strong>Is Private?:</strong> <%= isPrivate ? "Yes" : "No" %></p>
        <p><strong>Current users on whitelist:</strong>
          <% if (usersOnViewWhitelist && usersOnViewWhitelist.length > 0) { %>
            <%= usersOnViewWhitelist.join(", ") %>
          <% } else { %>
            None
          <% } %>
        </p>
      </div>
      <!-- Update Permissions Form -->
      <form action="<%= locals.linkToPost %>" method="post">
        <div>
          <label for="isPrivate">Is Private?</label>
          <input type="checkbox" id="isPrivate" name="isPrivate" value="true" <%= isPrivate ? "checked" : "" %>>
        </div>
        <div>
          <label for="addWhitelist">Add users to view whitelist (space-separated):</label>
          <input type="text" id="addWhitelist" name="addWhitelist">
        </div>
        <div>
          <label for="removeWhitelist">Remove users from view whitelist (space-separated):</label>
          <input type="text" id="removeWhitelist" name="removeWhitelist">
        </div>
        <div>
            <label for="password">Reset password:</label> <br>
            <input type="text" id="password" name="password">
          </div>
        <button type="submit">Update permissions</button>
      </form>
    </div>
  </div>

  <!-- Footer -->
  <footer>
    Uploading files since 2025
  </footer>
</body>
</html>
