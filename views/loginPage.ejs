<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <% if (locals.msg) { %>
        <div>
            <%= locals.msg %>
        </div>
    <% } %>
    <form method="post"> <!--no action so that by default it sends back to the page we were already on-->

        <label for="username">Username:</label>
        <input type="username", name="username">
        <br>

        <label for="password">Password:</label>
        <input type="password", name="password">
        <br>

        <!-- TODO: Implement button for forgetting your password, reset via email -->

        <button type="submit">Register now</button>
    </form>
</body>
</html>