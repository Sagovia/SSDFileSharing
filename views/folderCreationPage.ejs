<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="Content-Security-Policy" content="default-src 'self'; script-src 'self' 'nonce-<%= nonce %>'; style-src 'self' 'nonce-<%= nonce %>';">
  <title>Create Folder - SSDFileSharing</title>
  <style nonce="<%= nonce %>">

    html, body {
      height: 100%;
    }

    body {
      margin: 0;
      flex-direction: column;
      font-family: Arial;
      display: flex;
      background-color: whitesmoke;
      color: darkgray;
    }


    header {
      background: #005f99;
      color: whitesmoke;
      padding: 1.6rem 2rem;
      text-align: center;
    }

    header h1 {
      margin: 0;
      font-size: 2rem;
    }


    .container {
      flex: 1;
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 2rem;
    }


    .card {
      width: 100%;
      padding: 2rem;
      background: white;
      border-radius: 10px;
      max-width: 450px;
    }
    .card h2 {
      margin-top: 0;
      text-align: center;
      margin-bottom: 1.5rem;
      color: #005f99;
    }

    

    form {
      display: flex;
      flex-direction: column;
    }

    form button {
      background-color: #005f99;
      border: none;
      color: whitesmoke;
      padding: 0.7rem;
      font-size: 1rem;
      cursor: pointer;
      border-radius: 4px;
      margin-top: 1.6rem;
    }

 
    form input[type="text"],
    form input[type="password"] {
      border-radius: 4px;
      padding: 0.5rem;
      font-size: 1rem;
      border: 1px solid lightgray;
      margin-top: 0.5rem;
    }
    form label {
      color: #003d66;
      font-weight: bold;
      margin-top: 1rem;
    }
    form input[type="checkbox"] {
      margin-right: 0.5rem;
    }
    

    a {
      text-decoration: none;
      color: #005f99;
    }

    /* Toggle Whitelist Section with CSS: 
    Place the whitelist section immediately after the checkbox */
    .whitelist-group {
      display: none;
      flex-direction: column;
    }
    /* When the checkbox is checked, display the sibling .whitelist-group */
    #isPrivate:checked + .whitelist-group {
      display: flex;
    }



    footer {
      color: white;
      font-size: 1rem;
      padding: 1.3rem 2rem;
      background: #005f99;
      text-align: right;
    }
  </style>
</head>
<body>
  <!-- Header -->
  <header>
    <h1>Create a New Folder</h1>
  </header>

  <!-- Main Container -->
  <div class="container">
    <div class="card">
      <form action="/acc/createFolder" method="POST">
        <label for="name">Folder Name:</label>
        <input type="text" id="name" name="name" required>


        <!-- Private Folder Toggle -->
        <div style="margin-top: 1rem;">

          <label for="isPrivate">Private Folder?</label>
          <input type="checkbox" id="isPrivate" name="isPrivate" value="true">
        </div>

        <!-- Whitelist Section (toggle shown when checkbox is checked via CSS) -->
        <div class="whitelist-group">
          <label for="password">Provide folder password (optional):</label>
          <input type="text" id="password" name="password">

          <label for="viewWhitelistUsernames">View Whitelist (comma-separated usernames):</label>
          <input type="text" id="viewWhitelistUsernames" name="viewWhitelistUsernames">

          <label for="editWhitelistUsernames">Edit Whitelist (comma-separated usernames):</label>
          <input type="text" id="editWhitelistUsernames" name="editWhitelistUsernames">

          <label for="deleteWhitelistUsernames">Deletion Whitelist (comma-separated usernames):</label>
          <input type="text" id="deleteWhitelistUsernames" name="deleteWhitelistUsernames">
        </div>

        <button type="submit">Create Folder</button>
      </form>
    </div>
  </div>

  <!-- Footer -->
  <footer>
    Uploading files since 2025
  </footer>
</body>
</html>
