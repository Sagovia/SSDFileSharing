<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <% if(locals.fileLink != null ) { %>
        <div>
            Your file has been uploaded
            <!-- The = just says to print it out into the HTML -->
            <a href="<%= locals.fileLink%>"><%= locals.fileLink%></a>
        </div>
    
    <% } %>


    <form action="/upload" method="post" enctype="multipart/form-data">
        <label for="inputFile">Provide file:</label>
        <input type="file", name="file" required> <!-- Want this field to be required -->


        <label for="inputPassword">Provide password:</label>
        <input type="password", name="password"> <!-- Optional password, file doesn't need to be protected -->

        <button type="submit">Submit</button>
    </form>
</body>
</html>


