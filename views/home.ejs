<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="Content-Security-Policy" content="default-src 'self'; script-src 'self' 'nonce-<%= nonce %>'; style-src 'self' 'nonce-<%= nonce %>';">
  <title>SSDFileSharing</title>
  <style nonce="<%= nonce %>">

    

    html, body {
      height: 100%;
    }

    body {
      background-color: whitesmoke;
      margin: 0;
      color: darkgray;
      font-family: Arial;
    }

    a {
      color: white;
      text-decoration: none;
    }

    .topbar {
      display: flex;
      padding: 1rem 2rem;
      align-items: center;
      justify-content: space-between;
      color: white;
      background: #005f99;
    }
    .topbar .logo {
      font-size: 1.3rem;
      font-weight: bold;
    }
    .topbar .nav-links a {
      margin-left: 1.4rem;
      font-size: 1rem;
    }




    .upload-box {
      flex-direction: column;
      width: 200px;
      height: 200px;
      border-radius: 30%;
      align-items: center;
      cursor: pointer;
      display: flex;
      justify-content: center;
      background: white;
    }


    .upload-box i {
      color: #005f99;
      font-size: 3.5rem;
    }
    .upload-box span {
      font-size: 1rem;
      color: #005f99;
      margin-top: 0.5rem;
      font-weight: bold;
    }

    .upload-section {
      align-items: center;
      display: flex;
      margin: 3rem 0;
      justify-content: center;
    }


    .features {
      justify-content: space-around;
      padding: 2rem;
      display: flex;
      flex-wrap: wrap;
      margin: 2rem;
    }
    .feature {
      background: white;
      width: 320px;
      margin: 1rem;
      flex-direction: row;
      display: flex;
      border-radius: 10px;
    }

    .main-content {
      margin: 2rem;
      align-items: center;
      padding: 2rem;
      display: flex;
      justify-content: space-around;
      background: whitesmoke;
      border-radius: 8px;
    }
    .main-content .text {
      max-width: 500px;
      font-size: 1rem;
      color: #003d66;
    }
    .main-content .image {
      border-radius: 8px;
      max-width: 400px;
      height: auto;
    }

    .feature .feature-box {
      width: 100px;
      height: 100px;
      background: #005f99;
      background-size: cover;
      background-position: center;
    }
    .feature .feature-text {
      font-size: 1rem;
      color: #003d66;
      padding: 1rem;
      flex: 1;
      font-weight: bold;
      display: flex;
      align-items: center;
    }

    footer {
      display: flex;
      background: #005f99;
      color: white;
      padding: 1rem 2rem;
      justify-content: flex-end;
      font-size: 1rem;
    }
  </style>
</head>
<body>
  <!-- Top Navigation Bar -->
  <header class="topbar">
    <div class="logo">SSDFileSharing</div>
    <nav class="nav-links">
      <a href="<%= loginLink %>">Login</a>
      <a href="<%= registerLink %>">Register</a>
    </nav>
  </header>
  <section class="upload-section">
    <div class="upload-box" id="uploadBox">
      <span>Upload a file</span>
    </div>
  </section>

  <!-- Main Content Section -->
  <section class="main-content">
    <div class="text">
      <h1>A Secure, Collaborative File Sharing Solution</h1>
      <p>Share files easily and work on projects together.</p>
    </div>
    <div class="image">
      x
    </div>
  </section>

  <!-- Features Section -->
  <section class="features">
    <div class="feature">
      <div class="feature-box"></div>
      <div class="feature-text">Upload files securely</div>
    </div>
    <div class="feature">
      <div class="feature-box"></div>
      <div class="feature-text">Manage collaborative folders</div>
    </div>
    <div class="feature">
      <div class="feature-box"></div>
      <div class="feature-text">Manage access controls</div>
    </div>
  </section>

  <!-- Footer -->
  <footer>
    <div>Uploading files since 2025</div>
  </footer>

  <script nonce="<%= nonce %>">
    document.getElementById('uploadBox').addEventListener('click', function() {
      window.location.href = '<%= folderUploadLink %>';
    });
  </script>
</body>
</html>
